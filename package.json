{
  "name": "week9-server",
  "version": "1.0.0",
  "description": "Week 9 assignment server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx puppeteer browsers install chrome || echo 'Chrome installation skipped'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^23.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

