{
  "name": "week9-server",
  "version": "1.0.0",
  "description": "Week 9 assignment server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^23.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

